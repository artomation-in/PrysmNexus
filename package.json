{
  "name": "prysmnexus-astro-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1 --port 4174",
    "build": "astro build",
    "preview": "pnpm run build && wrangler dev",
    "check": "astro check",
    "generate-types": "wrangler types",
    "deploy": "pnpm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3",
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.0.7"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "sharp": "^0.35.3",
    "typescript": "6.0.3",
    "wrangler": "^4.112.0"
  }
}
